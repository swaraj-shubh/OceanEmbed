# core
numpy
pandas
xarray
netCDF4
zarr
scipy
# model
torch
# data access (accounts required: CMEMS, NASA Earthdata)
copernicusmarine
# argopy dropped: it pins an aiohttp with no wheel for recent Python and needs MSVC;
# src/download/argo.py hits the Ifremer ERDDAP endpoint directly instead
certifi
# viz / demo
matplotlib
cartopy
streamlit
plotly
